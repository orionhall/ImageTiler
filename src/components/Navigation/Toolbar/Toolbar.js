import React from 'react';
import Logo from '../Logo/Logo';

const Toolbar = () => {
    return (
        <header className="header">
            <Logo />
        </header>
    );
};

export default Toolbar;
